# 练习 01: 数据库连通性测试

## 任务目标
证明你的 MySQL 容器已经启动，并且能够正常接收和执行 SQL 语句。

## 题目说明
请在 `exercise.sql` 中编写一条 `SELECT` 查询语句。
要求查询输出一列名为 `message`，且值为 `Hello MySQL` 的字符串。

## 期望输出表结果
```text
message
Hello MySQL
```

## 运行方式
当你在 `exercise.sql` 中写好代码后，在聊天框向我发送：
```bash
/db.check
```
