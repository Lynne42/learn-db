# 练习 03: Docker 架构配置理解

## 任务目标
检验你对端口映射和持久化存储配置的掌握情况。

## 题目说明
请打开 `exercise.txt` 文件，在对应的横线处填写你的答案。

1. 根据我们的 `docker-compose.yml` 配置，MySQL 在容器内部的真实存储路径是：___________
2. 我们在宿主机（你的本机电脑）用来存放持久化数据的相对路径是：___________
3. 假设 `ports` 配置为 `"13306:3306"`，那么你在本机连接数据库时，填写的端口号应该是：___________

## 运行方式
填写完成后，在聊天框向我发送：
```bash
/db.check
```
